#cloud-config
package_update: true
package_upgrade: true

packages:
  - curl
  - htop

runcmd:
  - |
    curl -sfL https://get.k3s.io | \
      K3S_URL=https://${k3s_master}:6443 \
      K3S_TOKEN=${k3s_token} \
      sh -s - agent \
      --node-name=$(hostname)